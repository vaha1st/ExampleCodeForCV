<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans
    	http://www.springframework.org/schema/beans/spring-beans.xsd
    	http://www.springframework.org/schema/context
    	http://www.springframework.org/schema/context/spring-context.xsd
    	http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Добавление библиотеки для сканирования компонентов spring -->
    <context:component-scan base-package="com.vaha1st" />

    <!-- Поддержка конверсии, формата и валидации -->
    <mvc:annotation-driven/>

    <!-- Определение распознавателя MVC представлений -->
    <bean
            class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- Загрузка ресурсов для кастомизации сообщений -->
    <bean id="messageSource"
          class="org.springframework.context.support.ResourceBundleMessageSource">
        <!--Использована замена сообщения на ошибке несовпадения типа вводимых данных, если пользователь введет строку
        вместо цифр. src/resources/messages.properties -->
        <property name="basenames" value="resources/messages" />
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

</beans>