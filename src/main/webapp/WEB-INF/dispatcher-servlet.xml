<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/tx
		http://www.springframework.org/schema/tx/spring-tx.xsd
		http://www.springframework.org/schema/aop
		http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- Поддержка AspectJ autoproxy для AOP -->
    <aop:aspectj-autoproxy />

    <!-- Добавление библиотеки для сканирования компонентов spring -->
    <context:component-scan base-package="com.vaha1st" />

    <!-- Поддержка конверсии, формата и валидации -->
    <mvc:annotation-driven/>

    <!-- Определение распознавателя MVC представлений -->
    <bean
            class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- Поддержка hibernate -->
    <!-- Шаг 1: Обозначить Database DataSource / connection pool -->
    <bean id="myDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
          destroy-method="close">
        <property name="driverClass" value="com.mysql.cj.jdbc.Driver" />
        <property name="jdbcUrl" value="jdbc:mysql://localhost:3306/berkut_converter?useSSL=false&amp;serverTimezone=UTC" />
        <property name="user" value="berkutconverter" />
        <property name="password" value="berkutconverter" />

        <!-- Настройка пула подключений для C3P0 -->
        <property name="minPoolSize" value="5" />
        <property name="maxPoolSize" value="20" />
        <property name="maxIdleTime" value="30000" />
    </bean>

    <!-- Шаг 2: Настройка Hibernate фабрики сессий -->
    <bean id="sessionFactory"
          class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="myDataSource" />
        <property name="packagesToScan" value="com.vaha1st.entity" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.MySQLDialect</prop>
                <prop key="hibernate.show_sql">true</prop>
            </props>
        </property>
    </bean>

    <!-- Шаг 3: Настройка Hibernate менеджера транзакций -->
    <bean id="myTransactionManager"
          class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- Шаг 4: Активация конфигурации поведения транзакций путем аннотаций -->
    <tx:annotation-driven transaction-manager="myTransactionManager" />

    <!-- Поддержка web ресурсов: css, js, images... -->
    <mvc:resources location="/resources/" mapping="/resources/**"></mvc:resources>

    <!-- Загрузка ресурсов для кастомизации сообщений -->
    <bean id="messageSource"
          class="org.springframework.context.support.ResourceBundleMessageSource">
        <!--Использована замена сообщения на ошибке несовпадения типа вводимых данных, если пользователь введет строку
        вместо цифр. src/resources/messages.properties -->
        <property name="basenames" value="resources/messages" />
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

</beans>